import { ActionSheetIOS, Platform } from 'react-native';

export type RootStackParamList = {
    Login: undefined;
    Home: undefined;
};

// We will use React Navigation, but first we need to install it.
// I'll assume it's being installed now.
